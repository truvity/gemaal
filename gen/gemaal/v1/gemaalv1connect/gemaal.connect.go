// gemaal/v1 — the public API of the gemaal service.
//
// gemaal is a watcher, not a deployer: nothing in this API installs
// anything. Plan and Sweep are the janitor face; ListTenants the console
// face; Checkout and Extend the tenant-lifetime face; Resolve the
// identity authority. See docs/design.md for the tenancy model.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gemaal/v1/gemaal.proto

package gemaalv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/truvity/gemaal/gen/gemaal/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GemaalServiceName is the fully-qualified name of the GemaalService service.
	GemaalServiceName = "gemaal.v1.GemaalService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GemaalServicePlanProcedure is the fully-qualified name of the GemaalService's Plan RPC.
	GemaalServicePlanProcedure = "/gemaal.v1.GemaalService/Plan"
	// GemaalServiceListTenantsProcedure is the fully-qualified name of the GemaalService's ListTenants
	// RPC.
	GemaalServiceListTenantsProcedure = "/gemaal.v1.GemaalService/ListTenants"
	// GemaalServiceCheckoutProcedure is the fully-qualified name of the GemaalService's Checkout RPC.
	GemaalServiceCheckoutProcedure = "/gemaal.v1.GemaalService/Checkout"
	// GemaalServiceExtendProcedure is the fully-qualified name of the GemaalService's Extend RPC.
	GemaalServiceExtendProcedure = "/gemaal.v1.GemaalService/Extend"
	// GemaalServiceSweepProcedure is the fully-qualified name of the GemaalService's Sweep RPC.
	GemaalServiceSweepProcedure = "/gemaal.v1.GemaalService/Sweep"
	// GemaalServiceResolveProcedure is the fully-qualified name of the GemaalService's Resolve RPC.
	GemaalServiceResolveProcedure = "/gemaal.v1.GemaalService/Resolve"
	// GemaalServiceDecommissionProcedure is the fully-qualified name of the GemaalService's
	// Decommission RPC.
	GemaalServiceDecommissionProcedure = "/gemaal.v1.GemaalService/Decommission"
)

// GemaalServiceClient is a client for the gemaal.v1.GemaalService service.
type GemaalServiceClient interface {
	// Plan computes, without side effects, what housekeeping would do right
	// now. Every action carries the rule that fired and a human-readable
	// reason with the numbers in it.
	Plan(context.Context, *connect.Request[v1.PlanRequest]) (*connect.Response[v1.PlanResponse], error)
	// ListTenants enumerates the watched installations with their ledger
	// state (labels, effective TTL, pending action).
	ListTenants(context.Context, *connect.Request[v1.ListTenantsRequest]) (*connect.Response[v1.ListTenantsResponse], error)
	// Checkout claims a tenant slot for the caller, stamping keep-until so
	// the housekeeping loop leaves it alone for the requested window.
	Checkout(context.Context, *connect.Request[v1.CheckoutRequest]) (*connect.Response[v1.CheckoutResponse], error)
	// Extend pushes an existing tenant's keep-until forward.
	Extend(context.Context, *connect.Request[v1.ExtendRequest]) (*connect.Response[v1.ExtendResponse], error)
	// Sweep executes the current plan. The server's dry-run configuration
	// wins: a dry-run server reports what it would have done and deletes
	// nothing.
	Sweep(context.Context, *connect.Request[v1.SweepRequest]) (*connect.Response[v1.SweepResponse], error)
	// Resolve maps an employee email — extracted client-side by an
	// identity evidence driver — to the slug and namespace the service's
	// personnel-sourced configuration assigns it.
	Resolve(context.Context, *connect.Request[v1.ResolveRequest]) (*connect.Response[v1.ResolveResponse], error)
	// Decommission uninstalls ONE tenant's release ring now — the
	// explicit end-of-life call. The tenant's own keep-until does not
	// hold it (the caller is stating "done"), but everything else that
	// guards a sweep guards this: the caller must hold the same
	// namespace authorization Checkout requires, reach re-derives from
	// the server's configuration, ring3 drains before ring2, and the
	// server's dry-run setting wins. It does not consult the harness's
	// claim Lease: within one's own namespace this is the same power
	// `gemaalctl uninstall` already grants client-side.
	Decommission(context.Context, *connect.Request[v1.DecommissionRequest]) (*connect.Response[v1.DecommissionResponse], error)
}

// NewGemaalServiceClient constructs a client for the gemaal.v1.GemaalService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGemaalServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GemaalServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	gemaalServiceMethods := v1.File_gemaal_v1_gemaal_proto.Services().ByName("GemaalService").Methods()
	return &gemaalServiceClient{
		plan: connect.NewClient[v1.PlanRequest, v1.PlanResponse](
			httpClient,
			baseURL+GemaalServicePlanProcedure,
			connect.WithSchema(gemaalServiceMethods.ByName("Plan")),
			connect.WithClientOptions(opts...),
		),
		listTenants: connect.NewClient[v1.ListTenantsRequest, v1.ListTenantsResponse](
			httpClient,
			baseURL+GemaalServiceListTenantsProcedure,
			connect.WithSchema(gemaalServiceMethods.ByName("ListTenants")),
			connect.WithClientOptions(opts...),
		),
		checkout: connect.NewClient[v1.CheckoutRequest, v1.CheckoutResponse](
			httpClient,
			baseURL+GemaalServiceCheckoutProcedure,
			connect.WithSchema(gemaalServiceMethods.ByName("Checkout")),
			connect.WithClientOptions(opts...),
		),
		extend: connect.NewClient[v1.ExtendRequest, v1.ExtendResponse](
			httpClient,
			baseURL+GemaalServiceExtendProcedure,
			connect.WithSchema(gemaalServiceMethods.ByName("Extend")),
			connect.WithClientOptions(opts...),
		),
		sweep: connect.NewClient[v1.SweepRequest, v1.SweepResponse](
			httpClient,
			baseURL+GemaalServiceSweepProcedure,
			connect.WithSchema(gemaalServiceMethods.ByName("Sweep")),
			connect.WithClientOptions(opts...),
		),
		resolve: connect.NewClient[v1.ResolveRequest, v1.ResolveResponse](
			httpClient,
			baseURL+GemaalServiceResolveProcedure,
			connect.WithSchema(gemaalServiceMethods.ByName("Resolve")),
			connect.WithClientOptions(opts...),
		),
		decommission: connect.NewClient[v1.DecommissionRequest, v1.DecommissionResponse](
			httpClient,
			baseURL+GemaalServiceDecommissionProcedure,
			connect.WithSchema(gemaalServiceMethods.ByName("Decommission")),
			connect.WithClientOptions(opts...),
		),
	}
}

// gemaalServiceClient implements GemaalServiceClient.
type gemaalServiceClient struct {
	plan         *connect.Client[v1.PlanRequest, v1.PlanResponse]
	listTenants  *connect.Client[v1.ListTenantsRequest, v1.ListTenantsResponse]
	checkout     *connect.Client[v1.CheckoutRequest, v1.CheckoutResponse]
	extend       *connect.Client[v1.ExtendRequest, v1.ExtendResponse]
	sweep        *connect.Client[v1.SweepRequest, v1.SweepResponse]
	resolve      *connect.Client[v1.ResolveRequest, v1.ResolveResponse]
	decommission *connect.Client[v1.DecommissionRequest, v1.DecommissionResponse]
}

// Plan calls gemaal.v1.GemaalService.Plan.
func (c *gemaalServiceClient) Plan(ctx context.Context, req *connect.Request[v1.PlanRequest]) (*connect.Response[v1.PlanResponse], error) {
	return c.plan.CallUnary(ctx, req)
}

// ListTenants calls gemaal.v1.GemaalService.ListTenants.
func (c *gemaalServiceClient) ListTenants(ctx context.Context, req *connect.Request[v1.ListTenantsRequest]) (*connect.Response[v1.ListTenantsResponse], error) {
	return c.listTenants.CallUnary(ctx, req)
}

// Checkout calls gemaal.v1.GemaalService.Checkout.
func (c *gemaalServiceClient) Checkout(ctx context.Context, req *connect.Request[v1.CheckoutRequest]) (*connect.Response[v1.CheckoutResponse], error) {
	return c.checkout.CallUnary(ctx, req)
}

// Extend calls gemaal.v1.GemaalService.Extend.
func (c *gemaalServiceClient) Extend(ctx context.Context, req *connect.Request[v1.ExtendRequest]) (*connect.Response[v1.ExtendResponse], error) {
	return c.extend.CallUnary(ctx, req)
}

// Sweep calls gemaal.v1.GemaalService.Sweep.
func (c *gemaalServiceClient) Sweep(ctx context.Context, req *connect.Request[v1.SweepRequest]) (*connect.Response[v1.SweepResponse], error) {
	return c.sweep.CallUnary(ctx, req)
}

// Resolve calls gemaal.v1.GemaalService.Resolve.
func (c *gemaalServiceClient) Resolve(ctx context.Context, req *connect.Request[v1.ResolveRequest]) (*connect.Response[v1.ResolveResponse], error) {
	return c.resolve.CallUnary(ctx, req)
}

// Decommission calls gemaal.v1.GemaalService.Decommission.
func (c *gemaalServiceClient) Decommission(ctx context.Context, req *connect.Request[v1.DecommissionRequest]) (*connect.Response[v1.DecommissionResponse], error) {
	return c.decommission.CallUnary(ctx, req)
}

// GemaalServiceHandler is an implementation of the gemaal.v1.GemaalService service.
type GemaalServiceHandler interface {
	// Plan computes, without side effects, what housekeeping would do right
	// now. Every action carries the rule that fired and a human-readable
	// reason with the numbers in it.
	Plan(context.Context, *connect.Request[v1.PlanRequest]) (*connect.Response[v1.PlanResponse], error)
	// ListTenants enumerates the watched installations with their ledger
	// state (labels, effective TTL, pending action).
	ListTenants(context.Context, *connect.Request[v1.ListTenantsRequest]) (*connect.Response[v1.ListTenantsResponse], error)
	// Checkout claims a tenant slot for the caller, stamping keep-until so
	// the housekeeping loop leaves it alone for the requested window.
	Checkout(context.Context, *connect.Request[v1.CheckoutRequest]) (*connect.Response[v1.CheckoutResponse], error)
	// Extend pushes an existing tenant's keep-until forward.
	Extend(context.Context, *connect.Request[v1.ExtendRequest]) (*connect.Response[v1.ExtendResponse], error)
	// Sweep executes the current plan. The server's dry-run configuration
	// wins: a dry-run server reports what it would have done and deletes
	// nothing.
	Sweep(context.Context, *connect.Request[v1.SweepRequest]) (*connect.Response[v1.SweepResponse], error)
	// Resolve maps an employee email — extracted client-side by an
	// identity evidence driver — to the slug and namespace the service's
	// personnel-sourced configuration assigns it.
	Resolve(context.Context, *connect.Request[v1.ResolveRequest]) (*connect.Response[v1.ResolveResponse], error)
	// Decommission uninstalls ONE tenant's release ring now — the
	// explicit end-of-life call. The tenant's own keep-until does not
	// hold it (the caller is stating "done"), but everything else that
	// guards a sweep guards this: the caller must hold the same
	// namespace authorization Checkout requires, reach re-derives from
	// the server's configuration, ring3 drains before ring2, and the
	// server's dry-run setting wins. It does not consult the harness's
	// claim Lease: within one's own namespace this is the same power
	// `gemaalctl uninstall` already grants client-side.
	Decommission(context.Context, *connect.Request[v1.DecommissionRequest]) (*connect.Response[v1.DecommissionResponse], error)
}

// NewGemaalServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGemaalServiceHandler(svc GemaalServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	gemaalServiceMethods := v1.File_gemaal_v1_gemaal_proto.Services().ByName("GemaalService").Methods()
	gemaalServicePlanHandler := connect.NewUnaryHandler(
		GemaalServicePlanProcedure,
		svc.Plan,
		connect.WithSchema(gemaalServiceMethods.ByName("Plan")),
		connect.WithHandlerOptions(opts...),
	)
	gemaalServiceListTenantsHandler := connect.NewUnaryHandler(
		GemaalServiceListTenantsProcedure,
		svc.ListTenants,
		connect.WithSchema(gemaalServiceMethods.ByName("ListTenants")),
		connect.WithHandlerOptions(opts...),
	)
	gemaalServiceCheckoutHandler := connect.NewUnaryHandler(
		GemaalServiceCheckoutProcedure,
		svc.Checkout,
		connect.WithSchema(gemaalServiceMethods.ByName("Checkout")),
		connect.WithHandlerOptions(opts...),
	)
	gemaalServiceExtendHandler := connect.NewUnaryHandler(
		GemaalServiceExtendProcedure,
		svc.Extend,
		connect.WithSchema(gemaalServiceMethods.ByName("Extend")),
		connect.WithHandlerOptions(opts...),
	)
	gemaalServiceSweepHandler := connect.NewUnaryHandler(
		GemaalServiceSweepProcedure,
		svc.Sweep,
		connect.WithSchema(gemaalServiceMethods.ByName("Sweep")),
		connect.WithHandlerOptions(opts...),
	)
	gemaalServiceResolveHandler := connect.NewUnaryHandler(
		GemaalServiceResolveProcedure,
		svc.Resolve,
		connect.WithSchema(gemaalServiceMethods.ByName("Resolve")),
		connect.WithHandlerOptions(opts...),
	)
	gemaalServiceDecommissionHandler := connect.NewUnaryHandler(
		GemaalServiceDecommissionProcedure,
		svc.Decommission,
		connect.WithSchema(gemaalServiceMethods.ByName("Decommission")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gemaal.v1.GemaalService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GemaalServicePlanProcedure:
			gemaalServicePlanHandler.ServeHTTP(w, r)
		case GemaalServiceListTenantsProcedure:
			gemaalServiceListTenantsHandler.ServeHTTP(w, r)
		case GemaalServiceCheckoutProcedure:
			gemaalServiceCheckoutHandler.ServeHTTP(w, r)
		case GemaalServiceExtendProcedure:
			gemaalServiceExtendHandler.ServeHTTP(w, r)
		case GemaalServiceSweepProcedure:
			gemaalServiceSweepHandler.ServeHTTP(w, r)
		case GemaalServiceResolveProcedure:
			gemaalServiceResolveHandler.ServeHTTP(w, r)
		case GemaalServiceDecommissionProcedure:
			gemaalServiceDecommissionHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGemaalServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGemaalServiceHandler struct{}

func (UnimplementedGemaalServiceHandler) Plan(context.Context, *connect.Request[v1.PlanRequest]) (*connect.Response[v1.PlanResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gemaal.v1.GemaalService.Plan is not implemented"))
}

func (UnimplementedGemaalServiceHandler) ListTenants(context.Context, *connect.Request[v1.ListTenantsRequest]) (*connect.Response[v1.ListTenantsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gemaal.v1.GemaalService.ListTenants is not implemented"))
}

func (UnimplementedGemaalServiceHandler) Checkout(context.Context, *connect.Request[v1.CheckoutRequest]) (*connect.Response[v1.CheckoutResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gemaal.v1.GemaalService.Checkout is not implemented"))
}

func (UnimplementedGemaalServiceHandler) Extend(context.Context, *connect.Request[v1.ExtendRequest]) (*connect.Response[v1.ExtendResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gemaal.v1.GemaalService.Extend is not implemented"))
}

func (UnimplementedGemaalServiceHandler) Sweep(context.Context, *connect.Request[v1.SweepRequest]) (*connect.Response[v1.SweepResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gemaal.v1.GemaalService.Sweep is not implemented"))
}

func (UnimplementedGemaalServiceHandler) Resolve(context.Context, *connect.Request[v1.ResolveRequest]) (*connect.Response[v1.ResolveResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gemaal.v1.GemaalService.Resolve is not implemented"))
}

func (UnimplementedGemaalServiceHandler) Decommission(context.Context, *connect.Request[v1.DecommissionRequest]) (*connect.Response[v1.DecommissionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gemaal.v1.GemaalService.Decommission is not implemented"))
}
